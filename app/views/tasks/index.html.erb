<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task.completed ? "[x]" : "[ ]" %>
     <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>


<li><%= link_to "Create a new task", new_path %></li>
